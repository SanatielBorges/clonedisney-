{
  "name": "seu-projeto",
  "version": "1.0.0",
  "description": "",
  "dependencies": {
    "gulp": "^5.0.0",
    "gulp-sass": "^5.1.0"
  },
  "scripts": {
    "dev": "gulp watch",
    "build": "gulp",
    "teste": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "gulp-imagemin": "^4.0.2",
    "sass": "^1.75.0"
  }
}
